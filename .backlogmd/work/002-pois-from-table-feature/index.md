<!-- METADATA -->

```yaml
work: Feature - Points of Interest from BigQuery table
status: done
assignee: ""
```

<!-- DESCRIPTION -->

Implement the first domain-backed API endpoint using clean architecture and a single vertical slice (feature). The slice uses **Points of Interest (POI)** and **BigQuery** via `@google-cloud/bigquery`.

**Source of truth:** The API contract is defined in **`docs/openapi.yaml`**. All path, parameters, request/response shapes, and status codes must match that OpenAPI definition. Use it to derive types, validation, and tests.

<!-- CONTEXT -->
